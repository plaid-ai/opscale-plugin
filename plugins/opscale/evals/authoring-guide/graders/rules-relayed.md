---
type: llm
focus: last_message
---

PASS if the reply states that the entry file is skill.md with required name and description frontmatter, that support files live under references/, scripts/, or assets/, and that binary files are limited to xlsx, xls, pdf, and docx.
FAIL if the reply invents a different bundle layout, omits the binary-type restriction, or answers only with generic advice not tied to OPSCALE rules.
